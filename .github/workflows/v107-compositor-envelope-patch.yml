name: Temporary v1.0.7 compositor envelope patch

on:
  push:
    branches: ["fix/v1.0.7"]
    paths:
      - ".github/workflows/v107-compositor-envelope-patch.yml"
      - "scripts/patch-v107-compositor-envelope.py"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply deterministic compositor fix
        run: python3 scripts/patch-v107-compositor-envelope.py
      - name: Commit corrected compositor
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add web/src/compositor.ts server/pipeline/compositor.ts web/src/preview-runtime.ts
          git diff --cached --quiet && exit 0
          git commit -m "Use full bounded compositor spacing envelope"
          git push origin HEAD:fix/v1.0.7
