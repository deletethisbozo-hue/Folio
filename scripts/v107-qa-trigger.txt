Folio 1.0.7 diagnostic qualification trigger
Candidate behavior is unchanged from the em-dash breakpoint candidate.
This run adds exact web-compositor diagnostics for the first overfull breakpoint so the remaining English infeasibility can be measured rather than guessed.
All release quality gates remain unchanged: hyphen rate <= 0.45, streak <= 2, relaxed lines <= 2, zero emergency ragged lines, zero one-word final lines, bounded spacing/tracking/scaling, justified drop-cap opening lines.
