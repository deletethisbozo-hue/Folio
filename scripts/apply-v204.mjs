import fs from 'node:fs';

function read(path){ return fs.readFileSync(path,'utf8'); }
function write(path, content){ fs.writeFileSync(path, content); }
function replaceOnce(path, from, to){
  const source = read(path);
  if (!source.includes(from)) throw new Error(`Missing anchor in ${path}: ${from.slice(0,80)}`);
  write(path, source.replace(from, to));
}

// Version metadata.
for (const path of ['package.json','package-lock.json']) {
  let source = read(path);
  source = source.replaceAll('"version": "2.0.3"','"version": "2.0.4"');
  write(path, source);
}
replaceOnce('web/src/StartScreen.tsx','<div className="start-version">2.0.3</div>','<div className="start-version">2.0.4</div>');

// Keep the chosen icon inside the packaged ASAR and force the renderer window to use it too.
let pkg = JSON.parse(read('package.json'));
pkg.build.files = Array.from(new Set([...(pkg.build.files ?? []), 'assets/folio-icon.png']));
pkg.build.win.icon = 'assets/folio-icon.png';
write('package.json', JSON.stringify(pkg, null, 2) + '\n');
replaceOnce('electron/main.cjs','    show: false,\n    autoHideMenuBar: true,','    show: false,\n    autoHideMenuBar: true,\n    icon: path.join(appRoot, "assets", "folio-icon.png"),');

// Rich-text image metadata: scale, crop ratio and focal point survive autosave/reload.
let rich = read('web/src/rich-text.ts');
rich = rich.replace(
`function escapeMarkdownAlt(value: string): string {\n  return value.replace(/([\\\\\\]])/g, "\\\\$1");\n}\n`,
`function escapeMarkdownAlt(value: string): string {\n  return value.replace(/([\\\\\\]])/g, "\\\\$1");\n}\n\ntype IllustrationSpec = { scale: number; crop: boolean; ratio: string; x: number; y: number };\n\nfunction clampNumber(value: unknown, min: number, max: number, fallback: number): number {\n  const parsed = Number(value);\n  return Number.isFinite(parsed) ? Math.max(min, Math.min(max, parsed)) : fallback;\n}\n\nfunction parseIllustrationSpec(attrs = ""): IllustrationSpec {\n  const scale = clampNumber(attrs.match(/(?:^|\\s)width=(?:\\\"|')?(\\d{1,3})%/i)?.[1], 25, 100, 100);\n  const crop = /\\.folio-crop\\b/.test(attrs);\n  const ratio = attrs.match(/\\.folio-ratio-([a-z0-9-]+)/i)?.[1] ?? "4-3";\n  const x = clampNumber(attrs.match(/data-folio-x=(?:\\\"|')?(\\d{1,3})/i)?.[1], 0, 100, 50);\n  const y = clampNumber(attrs.match(/data-folio-y=(?:\\\"|')?(\\d{1,3})/i)?.[1], 0, 100, 50);\n  return { scale, crop, ratio, x, y };\n}\n\nfunction ratioCss(ratio: string): string {\n  if (ratio === "1-1") return "1 / 1";\n  if (ratio === "3-2") return "3 / 2";\n  if (ratio === "2-3") return "2 / 3";\n  if (ratio === "16-9") return "16 / 9";\n  return "4 / 3";\n}\n\nfunction illustrationMarkdownAttrs(spec: IllustrationSpec): string {\n  const classes = [".folio-illustration"];\n  if (spec.crop) classes.push(".folio-crop", `.folio-ratio-\${spec.ratio}`);\n  const attrs = [...classes, `width=\${Math.round(spec.scale)}%`];\n  if (spec.crop) attrs.push(`data-folio-x=\${Math.round(spec.x)}`, `data-folio-y=\${Math.round(spec.y)}`, `style=\\\"object-position:\${Math.round(spec.x)}% \${Math.round(spec.y)}%\\\"`);\n  return `{${attrs.join(" ")}}`;\n}\n\nfunction liveIllustrationSource(asset: string): string {\n  if (typeof document === "undefined") return asset;\n  const image = Array.from(document.querySelectorAll<HTMLImageElement>(".editor-illustration img[data-folio-asset]"))\n    .find((candidate) => candidate.dataset.folioAsset === asset);\n  return image?.src || asset;\n}\n\nfunction illustrationControls(spec: IllustrationSpec): string {\n  const ratios = [["1-1","1:1"],["4-3","4:3"],["3-2","3:2"],["2-3","2:3"],["16-9","16:9"]]\n    .map(([value,label]) => `<option value=\\\"\${value}\\\"\${value === spec.ratio ? " selected" : ""}>\${label}</option>`).join("");\n  return `<div class=\\\"editor-illustration-controls\\\" contenteditable=\\\"false\\\"><label>Size <input data-folio-control=\\\"scale\\\" type=\\\"range\\\" min=\\\"25\\\" max=\\\"100\\\" step=\\\"5\\\" value=\\\"\${spec.scale}\\\"></label><button type=\\\"button\\\" data-folio-control=\\\"crop\\\" aria-pressed=\\\"\${spec.crop}\\\">\${spec.crop ? "Crop on" : "Crop"}</button><select data-folio-control=\\\"ratio\\\" \${spec.crop ? "" : "disabled"} aria-label=\\\"Crop ratio\\\">\${ratios}</select><label class=\\\"crop-axis\\\">X <input data-folio-control=\\\"x\\\" type=\\\"range\\\" min=\\\"0\\\" max=\\\"100\\\" step=\\\"5\\\" value=\\\"\${spec.x}\\\" \${spec.crop ? "" : "disabled"}></label><label class=\\\"crop-axis\\\">Y <input data-folio-control=\\\"y\\\" type=\\\"range\\\" min=\\\"0\\\" max=\\\"100\\\" step=\\\"5\\\" value=\\\"\${spec.y}\\\" \${spec.crop ? "" : "disabled"}></label></div>`;\n}\n`);

rich = rich.replace(
`  if (element.hasAttribute("data-folio-illustration")) {\n    const image = element.querySelector<HTMLImageElement>("img[data-folio-asset]");\n    if (!image) return "";\n    const asset = image.dataset.folioAsset?.trim();\n    if (!asset) return "";\n    const alt = escapeMarkdownAlt(image.alt.trim() || "Illustration");\n    return \`\\n\\n![\${alt}](\${asset}){.folio-illustration}\\n\\n\`;\n  }`,
`  if (element.hasAttribute("data-folio-illustration")) {\n    const image = element.querySelector<HTMLImageElement>("img[data-folio-asset]");\n    if (!image) return "";\n    const asset = image.dataset.folioAsset?.trim();\n    if (!asset) return "";\n    const alt = escapeMarkdownAlt(image.alt.trim() || "Illustration");\n    const spec: IllustrationSpec = {\n      scale: clampNumber(element.getAttribute("data-folio-scale"), 25, 100, 100),\n      crop: element.getAttribute("data-folio-crop") === "true",\n      ratio: element.getAttribute("data-folio-ratio") || "4-3",\n      x: clampNumber(element.getAttribute("data-folio-x"), 0, 100, 50),\n      y: clampNumber(element.getAttribute("data-folio-y"), 0, 100, 50),\n    };\n    return \`\\n\\n![\${alt}](\${asset})\${illustrationMarkdownAttrs(spec)}\\n\\n\`;\n  }`);

rich = rich.replace(
`    const image = line.trim().match(/^!\\[([^\\]]*)\\]\\(([^)]+)\\)(?:\\{[^}]*\\})?$/);\n    if (image) {\n      flush();\n      const alt = image[1].trim() || "Illustration";\n      const asset = image[2].trim();\n      const src = resolveAsset ? resolveAsset(asset) : asset;\n      blocks.push(\`<figure class="editor-illustration" data-folio-illustration="true" contenteditable="false"><img src="\${escapeHtml(src)}" alt="\${escapeHtml(alt)}" data-folio-asset="\${escapeHtml(asset)}"><figcaption>\${escapeHtml(alt)}</figcaption><button type="button" class="editor-illustration-remove" aria-label="Remove illustration" title="Remove illustration">×</button></figure>\`);\n      continue;\n    }`,
`    const image = line.trim().match(/^!\\[([^\\]]*)\\]\\(([^)]+)\\)(?:\\{([^}]*)\\})?$/);\n    if (image) {\n      flush();\n      const alt = image[1].trim() || "Illustration";\n      const asset = image[2].trim();\n      const spec = parseIllustrationSpec(image[3] ?? "");\n      const src = resolveAsset ? resolveAsset(asset) : asset;\n      const cropStyle = spec.crop ? \`aspect-ratio:\${ratioCss(spec.ratio)};object-fit:cover;object-position:\${spec.x}% \${spec.y}%;\` : "";\n      blocks.push(\`<figure class="editor-illustration" data-folio-illustration="true" data-folio-scale="\${spec.scale}" data-folio-crop="\${spec.crop}" data-folio-ratio="\${spec.ratio}" data-folio-x="\${spec.x}" data-folio-y="\${spec.y}" contenteditable="false" style="width:\${spec.scale}%"><img src="\${escapeHtml(src)}" alt="\${escapeHtml(alt)}" data-folio-asset="\${escapeHtml(asset)}" style="width:100%;\${cropStyle}"><figcaption>\${escapeHtml(alt)}</figcaption>\${illustrationControls(spec)}<button type="button" class="editor-illustration-remove" aria-label="Remove illustration" title="Remove illustration">×</button></figure>\`);\n      continue;\n    }`);

rich = rich.replace(
`export function markdownToPreviewHtml(markdown: string, ornament = "❦"): string {\n  // Also heal hard breaks saved by Folio <=1.0.1. The Pandoc/Lua path applies\n  // the same repair, so immediate and authoritative previews agree.\n  const reflowed = markdown.replace(/ {2,}\\n(?=\\S)/g, "\\n");\n  return markdownToEditorHtml(reflowed, ornament)\n    .replace(/<button\\b[^>]*class="editor-scene-break-remove"[^>]*>.*?<\\/button>/g, "")\n    .replace(/class="editor-scene-break"/g, 'class="scene-break" role="separator"')\n    .replace(/\\scontenteditable="false"/g, "");\n}`,
`export function markdownToPreviewHtml(markdown: string, ornament = "❦"): string {\n  // Also heal hard breaks saved by Folio <=1.0.1. The Pandoc/Lua path applies\n  // the same repair, so immediate and authoritative previews agree.\n  const reflowed = markdown.replace(/ {2,}\\n(?=\\S)/g, "\\n");\n  return markdownToEditorHtml(reflowed, ornament, liveIllustrationSource)\n    .replace(/<div\\b[^>]*class="editor-illustration-controls"[^>]*>[\\s\\S]*?<\\/div>/g, "")\n    .replace(/<button\\b[^>]*class="editor-illustration-remove"[^>]*>.*?<\\/button>/g, "")\n    .replace(/<button\\b[^>]*class="editor-scene-break-remove"[^>]*>.*?<\\/button>/g, "")\n    .replace(/class="editor-scene-break"/g, 'class="scene-break" role="separator"')\n    .replace(/class="editor-illustration"/g, 'class="folio-illustration-preview"')\n    .replace(/\\scontenteditable="false"/g, "");\n}`);
write('web/src/rich-text.ts', rich);

// Browser-installed image controls. They mutate semantic figure metadata then
// dispatch an editor input event, so the existing autosave path remains authoritative.
write('web/src/illustration-controls.ts', `function clamp(value: string | null, min: number, max: number, fallback: number): number {\n  const parsed = Number(value);\n  return Number.isFinite(parsed) ? Math.max(min, Math.min(max, parsed)) : fallback;\n}\n\nfunction ratioCss(value: string): string {\n  if (value === "1-1") return "1 / 1";\n  if (value === "3-2") return "3 / 2";\n  if (value === "2-3") return "2 / 3";\n  if (value === "16-9") return "16 / 9";\n  return "4 / 3";\n}\n\nfunction refreshFigure(figure: HTMLElement): void {\n  const image = figure.querySelector<HTMLImageElement>("img[data-folio-asset]");\n  if (!image) return;\n  const scale = clamp(figure.dataset.folioScale ?? null, 25, 100, 100);\n  const crop = figure.dataset.folioCrop === "true";\n  const ratio = figure.dataset.folioRatio || "4-3";\n  const x = clamp(figure.dataset.folioX ?? null, 0, 100, 50);\n  const y = clamp(figure.dataset.folioY ?? null, 0, 100, 50);\n  figure.style.width = scale + "%";\n  image.style.width = "100%";\n  image.style.height = "auto";\n  image.style.aspectRatio = crop ? ratioCss(ratio) : "auto";\n  image.style.objectFit = crop ? "cover" : "contain";\n  image.style.objectPosition = crop ? x + "% " + y + "%" : "50% 50%";\n  figure.querySelectorAll<HTMLInputElement | HTMLSelectElement>("[data-folio-control]").forEach((control) => {\n    if (control.dataset.folioControl === "ratio" || control.dataset.folioControl === "x" || control.dataset.folioControl === "y") control.disabled = !crop;\n  });\n  const cropButton = figure.querySelector<HTMLButtonElement>("button[data-folio-control=\\"crop\\"]");\n  if (cropButton) { cropButton.textContent = crop ? "Crop on" : "Crop"; cropButton.setAttribute("aria-pressed", String(crop)); }\n}\n\nfunction markEditorDirty(figure: HTMLElement): void {\n  refreshFigure(figure);\n  const editor = figure.closest<HTMLElement>(".rich-editor");\n  editor?.dispatchEvent(new InputEvent("input", { bubbles: true, inputType: "formatSetBlockTextDirection" }));\n}\n\nexport function installIllustrationControls(): void {\n  document.addEventListener("click", (event) => {\n    const target = event.target as HTMLElement | null;\n    const control = target?.closest<HTMLElement>("[data-folio-control]");\n    const figure = control?.closest<HTMLElement>(".editor-illustration");\n    if (!control || !figure || control.dataset.folioControl !== "crop") return;\n    event.preventDefault();\n    figure.dataset.folioCrop = figure.dataset.folioCrop === "true" ? "false" : "true";\n    markEditorDirty(figure);\n  });\n  document.addEventListener("input", (event) => {\n    const control = (event.target as HTMLElement | null)?.closest<HTMLInputElement | HTMLSelectElement>("[data-folio-control]");\n    const figure = control?.closest<HTMLElement>(".editor-illustration");\n    if (!control || !figure) return;\n    const kind = control.dataset.folioControl;\n    if (kind === "scale") figure.dataset.folioScale = String(clamp(control.value, 25, 100, 100));\n    if (kind === "ratio") figure.dataset.folioRatio = control.value || "4-3";\n    if (kind === "x") figure.dataset.folioX = String(clamp(control.value, 0, 100, 50));\n    if (kind === "y") figure.dataset.folioY = String(clamp(control.value, 0, 100, 50));\n    markEditorDirty(figure);\n  }, true);\n}\n`);
replaceOnce('web/src/main.tsx','import { installRecentProjectTracking } from "./recent-projects";','import { installRecentProjectTracking } from "./recent-projects";\nimport { installIllustrationControls } from "./illustration-controls";');
replaceOnce('web/src/main.tsx','import "./v203-polish.css";','import "./v203-polish.css";\nimport "./v204-polish.css";');
replaceOnce('web/src/main.tsx','installRecentProjectTracking(api);','installRecentProjectTracking(api);\ninstallIllustrationControls();');

// Publishing CSS for persisted crop + scale metadata.
let baseCss = read('themes/base.css');
baseCss += `\n\n/* Folio inline illustration geometry. Width comes from Pandoc's width attribute;\n   crop classes preserve the editor's frame in reader and export output. */\nimg.folio-illustration.folio-crop { display:block; height:auto; object-fit:cover; overflow:hidden; }\nimg.folio-illustration.folio-crop.folio-ratio-1-1 { aspect-ratio:1 / 1; }\nimg.folio-illustration.folio-crop.folio-ratio-4-3 { aspect-ratio:4 / 3; }\nimg.folio-illustration.folio-crop.folio-ratio-3-2 { aspect-ratio:3 / 2; }\nimg.folio-illustration.folio-crop.folio-ratio-2-3 { aspect-ratio:2 / 3; }\nimg.folio-illustration.folio-crop.folio-ratio-16-9 { aspect-ratio:16 / 9; }\n`;
write('themes/base.css', baseCss);

// 2.0.4 UI polish: buttons, cover consistency, inline illustration controls.
write('web/src/v204-polish.css', `/* Folio 2.0.4 corrective polish. */\n.folio-shell[data-ui-tone] .generate-button {\n  min-width: 92px; height: 32px; padding: 0 13px; gap: 9px;\n  border: 1px solid var(--studio-rule-strong); border-radius: 6px;\n  background: color-mix(in srgb, var(--studio-surface) 94%, var(--studio-accent-soft));\n  color: var(--studio-ink); box-shadow: 0 1px 1px rgba(20,17,13,.05);\n  font: 700 12.5px/1 Georgia, \"Times New Roman\", serif; letter-spacing: -.01em;\n}\n.folio-shell[data-ui-tone] .generate-button::after { content: \"▾\"; font: 600 9px/1 system-ui,sans-serif; color: var(--studio-muted); transform:none; }\n.folio-shell[data-ui-tone] .generate-button:hover,\n.folio-shell[data-ui-tone] .generate-wrap:has(.generate-menu) .generate-button { border-color: var(--studio-accent); background: var(--studio-accent-soft); }\n\n.folio-shell[data-ui-tone] .cover-upload-button,\n.folio-shell[data-ui-tone] .cover-button {\n  min-height: 36px; padding: 0 16px; border: 1px solid var(--studio-rule-strong);\n  border-radius: 6px; background: color-mix(in srgb, var(--studio-surface) 96%, var(--studio-accent-soft)) !important;\n  color: var(--studio-ink); box-shadow: 0 1px 1px rgba(20,17,13,.05);\n  font: 700 12px/1 Georgia, \"Times New Roman\", serif; letter-spacing: -.01em;\n}\n.folio-shell[data-ui-tone] .cover-upload-button:hover,\n.folio-shell[data-ui-tone] .cover-button:hover { border-color: var(--studio-accent); background: var(--studio-accent-soft) !important; }\n\n/* The cover uses the complete reader screen as a contain box in every profile. */\n.folio-shell[data-ui-tone] .reader-screen > .cover-preview-surface { inset:0; padding:0; background:#f4f0e7; }\n.folio-shell[data-ui-tone] .cover-preview-surface img { width:100%; height:100%; max-width:100%; max-height:100%; object-fit:contain; margin:0; box-shadow:none; }\n\n.folio-shell[data-ui-tone] .editor-illustration { width:min(100%,520px); max-width:100%; min-width:180px; resize:none; }\n.folio-shell[data-ui-tone] .editor-illustration img { width:100%; max-width:100%; max-height:none; display:block; }\n.folio-shell[data-ui-tone] .editor-illustration-controls {\n  display:flex; flex-wrap:wrap; align-items:center; justify-content:center; gap:6px 8px;\n  margin-top:10px; padding:8px; border-top:1px solid var(--studio-rule);\n  color:var(--studio-muted); font:10px/1.2 \"Segoe UI\",sans-serif; user-select:none;\n}\n.folio-shell[data-ui-tone] .editor-illustration-controls label { display:flex; align-items:center; gap:5px; }\n.folio-shell[data-ui-tone] .editor-illustration-controls input[type=range] { width:84px; accent-color:var(--studio-accent); }\n.folio-shell[data-ui-tone] .editor-illustration-controls button,\n.folio-shell[data-ui-tone] .editor-illustration-controls select { height:25px; border:1px solid var(--studio-rule-strong); border-radius:4px; background:var(--studio-control); color:var(--studio-ink); font-size:10px; }\n.folio-shell[data-ui-tone] .editor-illustration-controls button { padding:0 8px; }\n.folio-shell[data-ui-tone] .editor-illustration-controls button[aria-pressed=true] { border-color:var(--studio-accent); background:var(--studio-accent-soft); }\n.folio-shell[data-ui-tone] .editor-illustration-controls :disabled { opacity:.38; }\n.folio-shell[data-ui-tone] .folio-illustration-preview { display:block; margin:1.4em auto; text-align:center; }\n.folio-shell[data-ui-tone] .folio-illustration-preview img { display:block; max-width:100%; margin:0 auto; }\n.folio-shell[data-ui-tone] .folio-illustration-preview figcaption { margin-top:.45em; text-align:center; font-size:.82em; color:#777; }\n`);

// Exact wording cleanup on visible controls.
let app = read('web/src/App.tsx');
app = app.replaceAll('"Replace Cover…"','"Replace cover"').replaceAll('"Add Cover…"','"Add cover"');
app = app.replaceAll('"Replace cover…"','"Replace cover"').replaceAll('"Add cover…"','"Add cover"');
write('web/src/App.tsx', app);

// Extend the browser test so the regression we just found is impossible to ship again.
let test = read('tests/illustration-ui.test.ts');
test = test.replace(
`  check("choosing a PNG inserts a visible illustration and semantic Markdown", true);`,
`  check("choosing a PNG inserts a visible illustration and semantic Markdown", true);\n\n  await page.waitForFunction(() => {\n    const frame = document.querySelector<HTMLIFrameElement>(".preview-frame");\n    const image = frame?.contentDocument?.querySelector<HTMLImageElement>(".folio-illustration-preview img, img.folio-illustration");\n    return Boolean(image?.complete && image.naturalWidth > 0);\n  }, { timeout: 30000 });\n  check("front-matter illustration is visible in live preview", true);\n\n  await page.$eval('.editor-illustration input[data-folio-control="scale"]', (input) => {\n    const range = input as HTMLInputElement; range.value = "65"; range.dispatchEvent(new Event("input", { bubbles: true }));\n  });\n  await page.$eval('.editor-illustration button[data-folio-control="crop"]', (button) => (button as HTMLButtonElement).click());\n  await page.$eval('.editor-illustration select[data-folio-control="ratio"]', (select) => {\n    const control = select as HTMLSelectElement; control.value = "1-1"; control.dispatchEvent(new Event("input", { bubbles: true }));\n  });\n  await page.waitForFunction(() => {\n    const markdown = (document.querySelector(".rich-editor") as HTMLElement | null)?.dataset.markdown ?? "";\n    return /width=65%/.test(markdown) && /\\.folio-crop/.test(markdown) && /\\.folio-ratio-1-1/.test(markdown);\n  });\n  check("illustration scale and crop settings persist into semantic Markdown", true);`);
write('tests/illustration-ui.test.ts', test);

// Release workflow becomes 2.0.4 and carries accurate notes.
let workflow = read('.github/workflows/windows-release.yml');
workflow = workflow.replaceAll('Folio 2.0.3','Folio 2.0.4').replaceAll('"2.0.3"','"2.0.4"');
workflow = workflow.replace(/--notes \"Folio 2\.0\.4[\s\S]*?hashing\.\"/,
  '--notes "Folio 2.0.4 fixes the remaining 2.0.3 UI/media regressions: Export and cover replacement controls are restyled, cover artwork uses one complete contain-fit contract across every preview profile, front-matter illustrations render in live preview immediately and now persist scale/crop/focal-point settings, and the chosen Folio icon is explicitly bundled and used by the Electron window as well as the Windows build. The release remains gated by typecheck, the complete formatter/UI suite on Windows, visual/typesetting QA, the full Print PDF matrix, packaged executable smoke tests, export checks and SHA-256 hashing."');
write('.github/workflows/windows-release.yml', workflow);

console.log('Applied Folio 2.0.4 patch.');
